# 麻雀スタッツ（スマホ入力アプリ）仕様書

## 1. 概要

| 項目 | 内容 |
|---|---|
| アプリ名 | 麻雀スタッツ（スマホ入力） |
| ファイル | `input-app.html`（単一HTMLファイル・シングルページアプリ、フレームワーク不使用） |
| 種別 | スマートフォン想定の単一HTMLファイル製Webアプリ（`viewport-fit=cover`・`apple-mobile-web-app-capable`対応。ホーム画面に追加してPWA的に使う運用を想定） |
| 目的 | 4人打ち麻雀の対局を局ごとにその場で記録し、半荘・通算単位でリーチ率／和了率／放銃率／平均順位などのスタッツを自動集計する |
| データ保存 | ブラウザの`localStorage`（端末内保存、キー接頭辞`mj-stats:`）のみ。クラウド同期・外部送信機能は持たない |
| 画面構成 | 下部タブバーで4画面を切り替え：①局入力　②半荘集計　③ランキング　④設定 |
| 局入力の2モード | 局入力タブ上部のセグメントコントロールで「簡易入力モード」「詳細入力モード」を切替可能（`inputMode`、`mj-settings`に保存）。簡易＝半荘終了時に最終持ち点のみ入力（旧「結果のみ入力」機能を昇格）。詳細＝一局ごとに直感的な麻雀卓UIで入力。既存の局を編集中は、`inputMode`の設定に関わらず常に詳細入力モードのUIを使う（`effectiveInputMode()`） |
| 配布・入手方法 | 公開サイトのトップページ（`index.html`）の「📱 入力アプリ」カードから起動できるほか、カード横の「⬇」ボタンで`input-app.html`単体をダウンロードすることもできる |

---

## 2. データモデル

アプリ内部は1つの`state`オブジェクトで全データを保持し、変更のたびに永続化する。

| キー | 内容 | 構造 |
|---|---|---|
| `players` | 現在の対局メンバー4人（新規入力に使う） | `[名前1, 名前2, 名前3, 名前4]`（配列の添字＝プレイヤーindex 0〜3。新規入力時にのみ使う値。過去の記録の表示には`recordPlayers`を使う） |
| `playerRoster` | 事前登録されたプレイヤー名リスト | `[string, ...]`。対局者4人はこのリストの中から選択する |
| `records` | 局ごとの記録（アプリの一次データ） | `{id, date, hanchanNo, kyoku, honba, resultType, stats}`の配列 |
| `ranks` | 半荘ごとの最終順位 | `{ "日付_半荘No": [P1〜P4の順位] }`（同点は同着＝同じ順位番号） |
| `finalScores` | 半荘ごとの最終持ち点（素点） | `{ "日付_半荘No": [P1〜P4の素点] }`（マイナス可の整数、未入力は`null`） |
| `finalPoints` | 半荘ごとの最終スコア（オカ・ウマ・チョンボ罰則を反映したpt） | `{ "日付_半荘No": [P1〜P4のpt] }`。順位確定時に自動算出 |
| `hanchanConfirmed` | 半荘ごとの確定フラグ | `{ "日付_半荘No": true/false }` |
| `dealers` | 半荘ごとの座席順（東家〜北家に誰が座っているか） | `{ "日付_半荘No": [東家idx, 南家idx, 西家idx, 北家idx] }` |
| `recordPlayers` | 半荘ごとのプレイヤー名スナップショット | `{ "日付_半荘No": [名前0〜名前3] }`（その半荘に最初の局を保存した時点の`players`を固定コピー） |
| `rankingRange` | ランキングタブの期間・曜日フィルタ | `{from, to, weekday}`（`from/to`は`YYYY-MM-DD`、`weekday`は`"all"｜"weekday"｜"weekend"`。画面表示用の一時状態、保存されない） |
| `current` | 局入力タブで編集中の局の状態 | `{date, hanchanNo, kyoku(0〜7のindex), honba, toggles}` |
| `editingId` / `editBackup` | 既存局を編集中かどうか、編集前の`current`退避 | 編集キャンセル時に復元するために使う |
| `scrollToHanchanKey` | 半荘集計タブへ自動遷移した際、スクロール先の半荘キー | 一時状態 |
| `hanchanCollapsed` | 半荘集計タブの確定済みカードの最小化状態 | `{ "日付_半荘No": false }`で明示的に展開。未設定時はデフォルト最小化。保存されない |
| `hanchanMonthFilter` | 半荘集計タブの年月絞り込み（`YYYY-MM`） | 一時状態、保存されない |
| `hanchanWeekdayFilter` | 半荘集計タブの曜日絞り込み | `"all"｜"weekday"｜"weekend"`。年月フィルタとAND条件。一時状態、保存されない |
| `storageOk` | `localStorage`書き込みに失敗した場合`false`になる内部フラグ | - |

### `records`（1レコード＝1局）の内訳

```
{
  id: 一意なID,
  date: "YYYY-MM-DD",
  hanchanNo: 半荘番号（1始まり）,
  kyoku: "東1"〜"南4" の文字列（"resultonly"の場合は "-" で局の指定なし）,
  honba: 積み場数（0始まり）,
  resultType: "next" | "ryuukyoku" | "renchan" | "chombo" | "resultonly",
  stats: [ [リーチ,副露,ツモ,ロン,放銃,チョンボ] ×4人分 ]   // 各0 or 1
}
```

`resultType`の意味：

| 値 | 意味 |
|---|---|
| `next` | 次の局へ（子のアガリが発生していた場合） |
| `ryuukyoku` | 次の局へ（誰もアガらず流局だった場合） |
| `renchan` | 連荘（親のアガリ・親テンパイ流局） |
| `chombo` | チョンボ（その局はやり直し。総局数・流局数のどちらにも含めない） |
| `resultonly` | 結果のみ入力（局の詳細を記録せず、半荘の最終順位・持ち点だけを記録する） |

### 2.1 プレイヤー名の扱い

- `players`は「今この瞬間の対局メンバー」であり、局入力タブでの新規入力にのみ使う。
- `recordPlayers`は各半荘に最初の局を記録した時点の`players`を固定コピーしたスナップショット。半荘集計・ランキングタブの表示は、すべてこの`recordPlayers`（`namesForHanchan()`関数）を経由して名前を取得する。これにより、設定タブで対局メンバーやプレイヤー名リストを後から変更しても、過去に記録済みの半荘の表示名は変わらない。
- 集計（`computeOverallStats`）はプレイヤーの「現在のindex(0〜3)」ではなく「名前（文字列）」をキーとして行う。同じ人物が異なるセッション・異なる座席indexで対局していても、名前が一致していれば正しく合算される。

---

## 3. 局入力タブ

### 3.1 簡易入力モード

- 日付（ステッパー＋`<input type="date">`）・半荘No（ステッパー）のみを設定し、大型ボタン「📋 半荘の結果を記録する」を押すと局の詳細を記録せず`resultonly`の1レコードのみを保存する（内部処理は`onSaveResultOnly`で従来の「結果のみ入力」と同じ）。保存後は即座に半荘集計タブへ遷移して最終持ち点の入力を促す（半荘集計タブの年月フィルタは自動解除され、対象半荘まで自動スクロールする）。
- 「直近の入力」カードは詳細入力モードと共通（3.3参照）。

### 3.2 詳細入力モード（直感麻雀卓UI）

- 画面上部：モード切替セグメント（簡易／詳細）の下に、現在の日付・半荘No・局・積み場を1行で示す要約バー（`.header-summary`）があり、タップすると日付ステッパー・半荘Noステッパー・座席（東家〜北家の4つの`<select>`、選択すると衝突する座席と自動で入れ替え。「🔀 シャッフル」でランダム再割当も可）・局（東1〜南4のボタングリッド、押すと積み場は0にリセット）・積み場（ステッパー）の詳細設定カードが開閉する（既定は折りたたみ、`tableHeaderCollapsed`。画面上のUI状態のみで保存はしない）。
- 画面中央に麻雀卓のミニマップ（`.table-wrap`／`.table-grid`）を配置し、プレイヤー配列順で固定した自家（P1・下部）・下家（P2・右）・対面（P3・上部）・上家（P4・左）の4枠と、中央に現在の局・積み場を表示するセルを配置する（中央セルタップでも要約バーと同じ折りたたみ開閉ができる）。
- 各プレイヤー枠には名前・座席上の位置ラベル・親バッジ（該当時）と、「リーチ」「鳴き（副露）」の2つのミニトグルボタンを表示する。タップでON/OFF、局が保存されて次局へ進むとリセットされる。相互排他制御（同一プレイヤー内でリーチ・副露は同時に成立しない）は`toggleStat`のまま。
- 画面下部に【ツモ】【ロン】【流局】の大型アクションボタン（`.action-bar`）と、その下に小さな「⚠️ チョンボを記録」リンクを配置する。
- 「直近の入力」カードは簡易入力モードと共通（3.3参照）。

### 3.3 入力フロー（和了者・放銃者・流局の選択オーバーレイ）

ツモ・ロン・流局・チョンボはいずれも、タップすると画面下からせり上がるボトムシート型のオーバーレイ（`state.overlay`。画面上のUI状態のみで保存はしない）で対象を選び、選択が完了した時点で即座に局を保存する（従来のような「連荘／次の局へ」ボタンによる手動選択は廃止し、和了者が親かどうかで自動判定する）。

- 【ツモ】タップ時：「和了者選択」（4人から1人）→ 選択したプレイヤーが親なら`renchan`、子なら`next`として即保存。
- 【ロン】タップ時：「和了者選択」（4人から1人）→「放銃者選択」（和了者を除く3人から1人）→ 和了者が親かどうかで`renchan`／`next`を自動判定して即保存。
- 【流局】タップ時：「親の聴牌有無」を選ぶ2択（聴牌＝`renchan`で連荘・積み場+1、ノーテン＝`ryuukyoku`で次局へ進行）。
- 【チョンボ】タップ時：チョンボしたプレイヤーを複数選択可能な一覧から選び、「この内容でチョンボを記録」で`chombo`として保存（同一局・同一積み場のままやり直しになる点は従来通り）。
- オーバーレイは「キャンセル」または背景タップで閉じられる。ロンの和了者選択後にキャンセルした場合など、フロー途中で仮に付いたツモ・ロン・放銃フラグ（リーチ・副露は対象外）は自動的にクリアされる。
- 保存直前に、ロンが成立しているのに放銃者が未設定という状態は起こり得ないが、念のため`validateAgariHoujuuConsistency`によるチェックは維持している。
- 「直近の入力」カードに直近5件の記録を新しい順に表示し、各行から編集（✎）・削除（×）ができる。編集（✎）を押すと、現在のモード設定に関わらず詳細入力モードの麻雀卓UIが開く（`effectiveInputMode`）。
- 南4局で局が終了する保存操作（連荘・チョンボ以外）を行うと、その半荘は終了とみなし、自動的に半荘集計タブへ遷移する（次の半荘No・東1・積み場0の状態に進む）。

---

## 4. 半荘集計タブ

### 4.1 年月フィルタ・曜日フィルタ

- タブ最上部に`<input type="month">`のフィルタバーを表示し、対象年月を選ぶと、その月に開始日付が属する半荘のみを一覧表示する。「すべて表示」ボタンでフィルタを解除する。
- 年月フィルタの下に「すべて／平日／週末」の切替チップ（`hanchanWeekdayFilter`）を表示する。日付から曜日を判定し（`isWeekendDate`：土・日を週末と判定）、年月フィルタと**AND条件**で絞り込む。
- 該当する半荘が0件の場合は、年月指定時は「◯年◯月に該当する半荘がありません」、年月指定なしで曜日フィルタのみ絞り込んでいる場合は「該当する半荘がありません」という空状態を表示する。
- 局入力タブから半荘終了・結果のみ入力で自動遷移する際は、遷移先の半荘が隠れないよう年月・曜日の両フィルタが自動解除される。
- これらの状態（`hanchanMonthFilter`／`hanchanWeekdayFilter`）は保存されない。

### 4.2 編集中（順位未確定）の半荘

- リーチ・副露・ツモ・ロン・放銃の局ごとの内訳テーブル（総局数・流局数を見出しに表示）を上部に表示する。
- 4人分の最終持ち点（素点）を`<input type="number" step="100">`で入力する。入力するたびに、合計と100,000点との差分をリアルタイム表示し、順位のプレビュー（`rank-chip`）と得点ptプレビューも表示する。
- 素点の合計が100,000点ちょうどになると「順位を確定する」ボタンが有効になる（`tryConfirmHanchan`）。
- 確定時、以下の計算式で得点ptを自動算出する（4.2節の計算式は9章参照）。
- 「結果のみ入力」で作成された半荘は座席データを持たないため、座席表示は常に「-」となる。
- カード内に「局ごとの記録」リストを表示し、その半荘を構成する各局の編集・削除ができる。
- カード右上の「🗑 削除」ボタンで、その半荘の記録（局データ・順位・持ち点・座席・名前スナップショット）を丸ごと削除する（要確認ダイアログ）。

### 4.3 確定済みの半荘

- デフォルトは最小化表示（見出し行のみ、タップで展開/折りたたみをトグル）。展開すると着順・座席・持ち点・得点pt・リーチ／副露／ツモ／ロン／放銃の内訳テーブルを表示する（着順の昇順で並べ替え）。
- チョンボが発生した対局者名の隣には「⚠️チョンボ×n」マークをインライン表示する（チョンボ罰則は得点ptに直接加算済みで、別集計項目としては扱わない）。
- 「編集」ボタンで確定状態を解除し（`hanchanConfirmed`を`false`に戻す）、素点の再入力ができる状態に戻る。「削除」ボタンは4.2と同じ。

---

## 5. ランキングタブ

- 通算ランキング表を1画面表示する。表示列は**順位／選手／累計pt／半荘数**のみ。
- `<input type="date">`×2で期間（開始日・終了日）を指定でき、指定した期間内に確定した半荘のみを対象に累計pt・半荘数を再集計する（`computeOverallStats(range)`）。未指定時は全期間が対象。
- 期間指定の下に「すべて／平日／週末」の切替チップ（`rankingRange.weekday`）を表示し、期間指定と**AND条件**で絞り込む（曜日判定ロジックは半荘集計タブと共通）。
- 対象は「確定済み半荘の順位・スコアがある全員」（`hanchanRankedCount>0`）。局入力の記録が無いプレイヤーでも、CSVで半荘結果のみを取り込んでいればここに表示される。
- 累計ptの降順に並べ、フォントは他タブの表より大きめ（`ranking-table-lg`）にして視認性を優先する。
- 対象期間（曜日フィルタ込み）に確定済み半荘が1件もない場合は「対象期間に確定済みの半荘がありません」と表示する。

---

## 6. 設定タブ

上から順に以下の4カードで構成される。

1. **プレイヤー名リスト（事前登録）**：テキスト入力＋「＋追加」ボタンで登録、チップの「×」で削除。
2. **対局メンバー（4人）**：プレイヤー名リストから今回の対局者4人を`<select>`で選択。ここでの変更は今後の入力にのみ適用され、過去に記録した半荘の選手名（半荘集計・ランキングタブでの表示）は変わらない旨を明示している。
3. **データ管理**：
   - 「CSVで書き出す」：局データをCSV形式でエクスポート（8章参照）
   - 「CSVから読み込む」：局データCSV、または半荘の順位・スコアのみを記録した過去大会データ（局の詳細なし）のいずれも同じ入力窓口で取り込める
   - 「🌐 サイト上のhanchan.csvを読み込む」：このアプリが配置されているサイトのルートにある`hanchan.csv`を`fetch`で取得し、上記と同じ取り込みロジック（`processCsvText`）に渡す。ファイルの選択操作なしに、サイト上に公開済みの最新CSVをその場で取り込める（読み込み専用。サーバーへの書き込みは行わない）
4. **データのバックアップ**：「📤 バックアップをダウンロード（JSON）」でエクスポート、「📥 バックアップから復元」で復元（現在の端末データを完全に上書き、要確認ダイアログ）。「🌐 サイト上のbackup.jsonから復元」でも同様に、サイトのルートにある`backup.json`を`fetch`で取得して復元できる（読み込み専用）。
5. **危険な操作**：「🗑 このアプリのデータを全削除する」（`localStorage`内の`mj-stats:`接頭辞キーのみを削除し、ページをリロードする。要確認ダイアログ）。

---

## 7. CSV・バックアップの入出力仕様

### 7.1 CSV書き出し（局データ）

- 1行＝1局のワイド形式。列構成：`日付,半荘No,局,本場,結果`＋（`P{n}_名前,P{n}_リーチ,P{n}_副露,P{n}_ツモ,P{n}_ロン,P{n}_放銃,P{n}_チョンボ,P{n}_素点`）×4人分。
- 素点列は、その半荘が「確定済み」の場合のみ、同じ半荘の全行に同じ値を繰り返し記載する（未確定なら空欄）。
- 局の詳細を持たない「結果のみ入力」の半荘も、局="-"・トグル全OFFの1行として同じ形式で書き出す。
- ファイル名は`hanchan.csv`固定、BOM付きUTF-8。

### 7.2 CSV読み込み

読み込みの起点はファイル選択（`importCsvFile`）とサイト上のCSV取得（`importCsvFromServer`）の2種類があるが、いずれもテキストを受け取った後は共通の`processCsvText`関数でヘッダーの内容から自動判定する。

- `importCsvFile(file)`：ファイル選択（`<input type="file">`）で選んだファイルを`FileReader`で読み込む。
- `importCsvFromServer()`：このアプリが配置されているサイトと同じオリジンの`./hanchan.csv`を`fetch(..., {cache:"no-store"})`で取得する（キャッシュを使わず常に最新を取得）。取得に失敗した場合はトースト表示のみでデータは変更しない。

- **ヘッダーに「半荘ID」列がある場合**：局の詳細を持たない過去大会の結果データ（ロング形式：半荘ID・日付・半荘No・プレイヤー・素点などの列）とみなして読み込む。半荘ID（例："20240911-01"）から日付・半荘Noを復元できる。素点・順位はファイル内の値を使わず、このアプリの計算式で必ず再計算する。取り込まれた半荘は「結果のみ入力」として扱われ、統計の打点系スタッツには含めない。4人分そろっていて合計100,000点の半荘のみを新規取り込み対象とし、既存と重複するキーはスキップする。
- **それ以外の場合**：局データのワイド形式として読み込む。現行形式（8列/人：名前+6スタッツ+素点）に加え、旧形式（7列/人：名前+6スタッツのみ）・最旧形式（6列/人：スタッツのみ、名前列なし）にもフォールバック対応する。素点が4人分そろっていて未確定の半荘は、取り込み時にこのアプリの計算式で自動確定する。日付・半荘No・局・本場・結果種別・各プレイヤーのスタッツがすべて一致する行は、既存データと重複する局とみなして取り込まない（同じCSVを誤って複数回取り込んでも二重登録されない）。チョンボ後のやり直しなど、局・本場が同じでも結果種別やスタッツが異なる行は別レコードとして扱う。

### 7.3 バックアップJSON

- `{app:"mahjong-stats-backup", version:3, exportedAt, players, playerRoster, records, ranks, dealers, hanchanConfirmed, finalScores, finalPoints, recordPlayers}`の全データスナップショット。
- ファイル名は`backup.json`固定。
- 復元時は現在の端末データを完全に上書きする（要確認ダイアログ）。
- 読み込みの起点はファイル選択（`importBackupFile`）とサイト上のJSON取得（`importBackupFromServer`）の2種類があるが、いずれもテキストを受け取った後は共通の`processBackupText`関数で検証・復元する。`importBackupFromServer()`は同じオリジンの`./backup.json`を`fetch(..., {cache:"no-store"})`で取得する。取得・形式検証に失敗した場合はトースト表示のみでデータは変更しない。

---

## 8. 計算ロジック

### 8.1 得点pt換算

配給原点25,000点・オカ30,000点・ウマ（+50,000／+10,000／▲10,000／▲30,000）を用いて、4人分の素点から最終スコア（pt）を自動算出する。

`pt = (素点 - 30,000 + ウマ) / 1,000`

同着（同点）の場合は該当するウマを均等に分配し、割り切れない端数は起家（東家）に近い座席の対局者から優先的に多く取得する。

### 8.2 チョンボ罰則

本人-15pt、他3名に+5ptずつ（ゼロサム）。半荘の得点pt（`finalPoints`）に直接加算され、独立した集計項目としては扱わない。チョンボは総局数・流局数のいずれの分母からも除外する。

### 8.3 総局数・流局数の判定

`resultType`だけに頼らず、実際のツモ・ロンの記録有無で流局を判定する（連荘ボタンが「親のアガリ」と「親テンパイ流局」の両方をカバーするため）。チョンボ・「結果のみ入力」はどちらの分母にも含めない。

### 8.4 集計の対象範囲（`computeOverallStats`）

- 打点系スタッツ（リーチ率・和了率・放銃率など）の分母は`kyokuCount`（局の詳細を伴う記録のみ。チョンボ・結果のみ入力は除外）。
- 着順・スコア系スタッツ（平均順位・トップ率・累計pt等）は「確定済み」の半荘（`hanchanConfirmed`）のみを対象にする。
- `playerRoster`に登録されている名前は、対局実績が無くても集計結果の一覧に含める（ランキングタブでは`hanchanRankedCount>0`の人のみ表示対象になる）。

---

## 9. 技術仕様

- 単一HTMLファイル、Vanilla JS（フレームワーク不使用）。
- 画面はすべて`innerHTML`の再描画＋イベント再バインド方式（`render()`を呼ぶたびに`#app`の中身を作り直す）。
- データ永続化は`localStorage`のみ（キー接頭辞`mj-stats:`）。クラウド保存・外部送信は行わない。
- 外部ライブラリ・CDN依存なし。オフラインでも動作する。

---

## 10. input-app-upload.html（GitHub連携版）

`input-app.html`を複製した派生ファイル。局入力・半荘集計・ランキングの機能・データモデルは本体と完全に同一。差分は以下の2点のみ。

### 10.1 差分：設定タブへの「GitHub連携（自動アップロード）」カード追加

- 入力欄：Personal Access Token（`type=password`）、owner、repo、ファイルパス（既定値：`owner=ikiym2206-masu`、`repo=masu-mahjongstats`、`path=backup.json`）
- 「GitHub設定を保存」：`localStorage`（キー`mj-stats:mj-github-settings`）に保存。本体の全削除（「このアプリのデータを全削除する」）でも同じ接頭辞のためあわせて削除される。
- 「⬇️ 最新をGitHubから取得」：手動プルボタン（10.3参照）。
- 「⬆️ 今すぐGitHubへ送信」：デバウンス待ちなしで即座にプル・マージ→プッシュを実行する手動ボタン。
- 同期状況表示（未設定／同期中／最終同期日時／前回のエラー内容）。

### 10.2 差分：保存経路へのGitHub自動同期フック（プル・マージ→プッシュ方式）

- `storageSet()`（全ての`save*`関数が経由する共通の永続化関数）に`scheduleGithubSync()`を追加。局の保存・編集・削除・CSV/バックアップ取り込みなど、`localStorage`へ書き込みが発生するすべての操作が対象になる。
- トークン・owner・repo・pathが4つとも設定されている場合のみ動作し、いずれか未設定なら何もしない（従来どおりローカル保存のみにフォールバック）。
- 操作が1.2秒止まってから1回だけ同期する（デバウンス）。連続入力時に無駄なAPI呼び出しが増えないようにするため。
- **プッシュ前にプル・マージする**：`uploadBackupToGithub`は`sha`取得と同時にGitHub上の最新内容も取得し、`mergeRemoteIntoState`でローカルへ取り込んでから、マージ後の内容をコミットする。これにより、他の端末が先に保存していた記録を、自分の保存で消してしまう事故を防ぐ。
- **マージ方針**：`records`はid基準の和集合（同じidはGitHub側を採用、ローカルにしかないidのみ追加）。半荘キー付きマップ（ranks/dealers/finalScores/finalPoints/hanchanConfirmed/recordPlayers）はGitHub側をベースに、ローカルにしかないキーのみ追加。プレイヤー名リスト・対局メンバーはGitHub側を優先。同じレコード／同じ半荘キーを複数端末がほぼ同時に編集した場合はGitHub側が優先され、その場合のみローカルの変更分が失われる（身内の少人数運用を前提に、完全な競合解決までは行わない既知の制約）。
- 書き込み競合（`PUT`が409）の場合、最新の`sha`を取り直して1回だけ自動リトライする。
- トークン無効・権限不足（401/403）はエラーメッセージをトースト表示し、次回の変更でも同期を再試行する。

### 10.3 差分：アプリ起動時・復帰時の自動プル

- アプリ起動直後（`loadAll()`完了後）に1回、GitHub上の最新backup.jsonを取得してマージする（`pullLatestFromGithub({initial:true})`）。このときのみ、マージ後の記録を基準に入力再開位置（日付・半荘No・局・本場）を再計算する。
- ブラウザ／PWAがバックグラウンドから復帰したとき（`visibilitychange`）にも同様にプルする（`silent:true`。エラーはトースト表示しない）。入力再開位置の再計算は行わない（入力途中の状態を壊さないため）。
- 設定タブの「⬇️ 最新をGitHubから取得」ボタンで、任意のタイミングで手動プルもできる。
- マージの結果、GitHub側に新しい記録があればトースト表示する。ローカルにしかなかった記録があれば、それを自動でGitHubへ送信する。

### 10.4 想定運用

- 複数メンバーがそれぞれ自分のGitHub Personal Access Token（Fine-grained、対象リポジトリのみ・`Contents: Read and write`権限）を発行し、`input-app-upload.html`の設定タブに1回だけ登録する。
- 以降は入力・保存するだけで、GitHub上の`backup.json`が自動更新される。ファイルの手渡しは不要。
- `viewer-app.html`は従来どおり`backup.json`を読み込むだけなので変更不要（GitHub上のファイルが更新されていれば、サイトの再デプロイ後に反映される）。
