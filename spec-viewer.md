# 麻雀スタッツ（PC分析ビューア）仕様書

## 1. 概要

| 項目 | 内容 |
|---|---|
| アプリ名 | 麻雀スタッツ（PC分析ビューア） |
| ファイル | `viewer-app.html`（単一HTMLファイル、フレームワーク不使用） |
| 種別 | PC画面を前提にした単一HTMLファイル製Webアプリ |
| 位置づけ | 入力アプリのバックアップJSONを読み込んで表示する、閲覧・分析専用ビューア |
| データ入力 | なし。入力アプリの「データのバックアップ」機能で書き出したJSONファイルを読み込んで表示するのみ |
| 永続化 | なし（`localStorage`不使用）。読み込んだデータはページを閉じる・再読み込みすると消える |
| 入力アプリ本体への影響 | なし。独立したファイルとして提供。本書の変更内容は入力アプリ側の改修を必要としない（後述9.1参照） |
| 配布・入手方法 | 公開サイトのトップページ（`index.html`）の「📊 分析ビューア」カードから起動できるほか、カード横の「⬇」ボタンで`viewer-app.html`単体をダウンロードすることもできる |

---

## 2. データ読み込み

### 2.1 起動時（未読込状態）

- 画面中央に「ファイルを選択」ボタン＋ドラッグ&ドロップ領域（`#dropzone`）、および「🌐 サイト上のbackup.jsonを読み込む」ボタン（`loadFromServer`）を表示する。
- 対象は入力アプリのバックアップJSON。読み込んだJSONの`app`キーが`"mahjong-stats-backup"`と一致しない場合、またはJSONとして不正な場合はエラーメッセージを表示し読み込みを中止する（この検証・反映処理はファイル選択／ドラッグ&ドロップ／サーバー読み込みの3経路で共通の`processBackupText`関数が行う）。
- 「🌐 サイト上のbackup.jsonを読み込む」は、このビューアが配置されているサイトと同じオリジンの`./backup.json`を`fetch(..., {cache:"no-store"})`で取得し、事前のダウンロード操作なしにその場で読み込む。取得に失敗した場合はローダー画面にエラーメッセージを表示する（読み込み専用。書き込みは行わない）。
- 読み込んだデータはページを閉じる・再読み込みすると消去される旨を画面に明記している。

### 2.2 読込後

- 読込に成功すると、表示状態（`section`・選択チップ・比較表対象・期間フィルタ・曜日フィルタ・相性レーダー対象・成績5段階評価の対象／タブ・統計カードの折りたたみ状態）はすべて初期化される。
- サイドバー最上段に読込サマリを常時表示：書き出し日時（`exportedAt`）／半荘件数（確定済み／全体、下記2.4のフィルタ適用中は「絞込後」の件数も追加表示）／分析対象プレイヤー数（対局・半荘いずれかの実績があるプレイヤー数）。
- サイドバー最下部の「📂 別のファイルを読み込む」ボタンで、確認ダイアログ後にファイル未読込の状態へ戻れる。

### 2.3 集計対象の基準

- 入力アプリの計算ロジック（`computeOverallStats` / `namesForHanchan` / `hanchanGroups` / `computeHanchanKyokuStats` / `computeHeadToHead`）をそのまま移植している。
- 着順系スタッツ（平均順位・トップ率など）は確定済み半荘（`hanchanConfirmed`）のみが対象。
- チョンボ・「結果のみ入力」の局は総局数・和了率・放銃率などの分母から除外する（得点計算のみに影響する）。
- 「統計」セクションのみ`computeOverallStats()`（プレイヤー名リストに登録されているだけで対局実績が無い人も含む）を対象にし、他の4セクション（①〜④）は`computeActiveStats()`（対局実績`kyokuCount>0`または半荘実績`hanchanRankedCount>0`があるプレイヤーのみ）を対象にする。
- `computeOverallStats()`は引数を取らず、常に下記2.4の期間・曜日フィルタ（`passesFilters`）を内部で適用した集計結果を返す。

### 2.4 期間・曜日フィルタ（全セクション共通、サイドバー設置）

- サイドバーの読込サマリ直下に「📅 期間・曜日フィルタ」欄を設置し、開始日・終了日（`state.dateRange`）と「すべて／平日／週末」の切替ボタン（`state.dayFilter`）を1箇所にまとめている（以前の版にあった①比較表・スコア推移それぞれ独自の期間指定は廃止し、この共通フィルタに統合済み）。
- 統計／①比較表／②スコア推移／③相性レーダーチャート／④成績5段階評価の**全セクションに共通で適用**される（`passesFilters(date) = inDateRange(date) && matchesDayFilter(date)`）。
- いずれかの条件が有効なときは「フィルタ解除」ボタンが表示され、両方まとめてクリアできる。
- サイドバー最上段の読込サマリ（半荘件数：確定済み／全体）は、このフィルタの影響を受けない（読み込んだファイル自体の件数を常に表示する）。フィルタが有効なときのみ、絞込後の確定済み件数を追加で表示する。
- 別ファイルの読込時（`processBackupText`）は両方とも初期値（期間指定なし・すべて）にリセットされる。画面表示用の一時状態のみで、保存はされない。

---

## 3. 画面構成

サイドバーナビゲーション形式。左サイドバーで4セクション（＋統計）を切り替え、メインエリアには選択中の1セクションのみを表示する。

```
┌──────────────┬─────────────────────────────┐
│ [読込サマリ]   │                              │
│ [期間・曜日フィルタ]│                        │
│──────────────│                              │
│ 📊 統計        │                              │
│ ① 比較表       │       メインエリア            │
│ ② スコア推移    │ （選択中の1セクションを全画面表示） │
│ ③ 相性レーダーチャート│                        │
│ ④ 成績5段階評価（相対評価）│                  │
│──────────────│                              │
│ [別ファイルを読込] │                          │
└──────────────┴─────────────────────────────┘
```

- 「統計」セクションで対局実績・半荘実績のあるプレイヤーが1人もいない場合は「プレイヤーが登録されていません」を表示する。
- 「統計」以外のセクションで対象プレイヤーが0人の場合は「対局実績のあるプレイヤーがいません。」（フィルタ適用中はサイドバーのフィルタ確認を促す文言を追加）を表示する。
- 900px以下の画面幅では、サイドバーが上部に横並びレイアウトへ切り替わる簡易レスポンシブ対応がある。

### 3.1 選択チップ（②スコア推移で使用）

- メインエリア上部、②スコア推移を表示しているときのみ見える位置に「選択中プレイヤー」のチップバー（`renderChipBar`）を表示する（①③④では非表示。①・③・④はいずれも独自のチェックボックス式対象選択を持つため）。
- チップに追加された選手には、選択した順に固定パレット（`CHART_PALETTE`、8色）から色を割り当てる（同時ハイライト数が8人を超えると色が循環する）。未選択のプレイヤーは共通のニュートラルグレー（`#B9BEC5`）で表示する。
- チップ操作の起点：②スコア推移グラフの凡例／折れ線のクリック（`state.chips`）。
- チップ横の「×」で個別解除、チップバー全体に「選択をクリア」ボタンがある。
- 共有の選択状態（`state.chips`）という設計自体は複数セクションでの再利用を想定したものだが、レーダーチャート比較・半荘目別推移の両セクションが廃止された現在は②スコア推移のみが利用している。

---

## 4. 「統計」セクション

入力アプリの半荘集計ロジックを移植した、プレイヤーごとの詳細スタッツカード画面。

1. **全プレイヤー平均値カード**（`📊 全プレイヤー平均値`）：局入力実績（`kyokuCount>0`）のあるプレイヤー全員の打点系スタッツの単純平均。デフォルト最小化、タップで展開。
2. **プレイヤーごとのスタッツカード**（登録済み・実績のある全プレイヤー分。CSS上は`stats-grid`でカード幅360px以上のグリッド表示）：デフォルト最小化。展開すると以下を表示する。
   - **着順**（半荘◯回分と明記）：平均順位／トップ率／2着率／3着率／ラス率／ハコ率
   - **打点**：総局数（詳細）／リーチ数／副露局数／和了数／ツモ数／ロン数／放銃数／リーチ率／副露率／和了率／放銃率／リーチ後和了率／リーチ後放銃率／副露後和了率／副露後放銃率
3. **用語説明**：和了・放銃・副露・チョンボの簡易定義をカード下部に表示。

「総局数（詳細）」は、`resultType`が`chombo`（チョンボ・やり直し）でも`resultonly`（結果のみ入力）でもない、局の詳細を伴う対局のみを数えた値。

---

## 5. ①比較表

- 指標は3つのグループに分かれ、それぞれ見出し行クリックで折りたたみ／展開できる（`COMPARE_METRIC_GROUPS`、デフォルト全展開、状態は`state.compareGroupCollapsed`）。
  - **着順系**：半荘数／平均順位／トップ率／2着率／3着率／ラス率
  - **打ち筋系**：総局数／和了数・和了率／放銃数・放銃率／リーチ数・リーチ率／副露数・副露率、およびサブグループ「リーチ後」「副露後」（それぞれ和了率／放銃率／流局率）
  - **和了／放銃　内訳**：サブグループ「和了内訳」「放銃内訳」「放銃相手内訳」（各リーチ／副露／ダマテンの回数・率）。サブグループ見出しには`COMPARE_SUBGROUP_DESC`による1行説明を添える。
- 行＝指標（`COMPARE_METRICS`）、列＝選択した選手（＋任意で「平均」列）。
- 表の文字サイズは他のミニテーブルより大きめ（セル本体15px・見出し12.5px程度。480px以下の狭い画面ではさらに縮小）にして視認性を高めている。`table-layout:fixed`により「指標」列＋選手列の合計幅を常にコンテナ幅（スマホ画面）に収め、文字が入りきらない場合は横スクロールではなく折り返しで対応する。
- 対象プレイヤーをチップ（チェックボックス）で選択する。対局・半荘実績のあるプレイヤーが6人（`COMPARE_DEFAULT_TARGET_COUNT`）以下の場合は初期状態で全員を選択済みにし、7人以上の場合は半荘数（`hanchanRankedCount`）上位6人を初期選択とする。チップで自由に増減できる（人数の上限はない）。「📊 平均」チップを選ぶと、対象プレイヤー全員の単純平均を仮想的な1列として追加できる。
- 先頭の「指標」列は`position:sticky; left:0;`で固定し、背景は不透明色（`var(--surface)`／`var(--bg)`）を指定して横スクロール時に他の列の文字が透けて重ならないようにしている。
- 3章の選択チップ（`state.chips`）に含まれる選手の列は、見出しとセルの背景・文字色をチップと同じ色でハイライトする（①自体からチップの追加・削除操作はできない。①はチェックボックスによる表示対象選択のみ）。
- 各指標行内で、選択中の選手（「平均」列を除く）の中の最大値・最小値のセルを色分け表示する。
- 表右上の「📋 表をコピー」ボタンで、現在選択中の選手・指標をタブ区切りテキストとしてクリップボードにコピーする（Excel等に貼り付け可能）。
- 対象プレイヤーが2人未満の場合は「対局実績のあるプレイヤーが2人以上必要です。」を表示する（フィルタのコントロールは表示したまま）。プレイヤーは2人以上いるが対象プレイヤーチップが1人も選択されていない場合は「プレイヤーを1人以上選択してください。」を表示する。

---

## 6. ②スコア推移

- 入力アプリの累計得点グラフをPC向けに移植・拡大したもの。
- 独自の期間指定は持たず、サイドバー共通の期間・曜日フィルタ（2.4節）で絞り込んだ範囲の確定済み半荘のみを対象にする。
- 対象期間に登場する全プレイヤー名について、確定済み半荘の得点pt（`finalPoints`）を日付順に累積した折れ線を描画する（期間より前の半荘の得点は含めない＝期間の先頭で0からの推移になる）。
- 3.1節の選択チップと連動：未選択はニュートラルグレーの細線、選択中は固有色の太線。凡例・折れ線のクリックでチップをON/OFF可能。
- 対象期間に確定済み半荘が1件もない場合は「対象期間に確定済みの半荘がありません。サイドバーの期間・曜日フィルタをご確認ください。」を表示する。

---

## 7. ③相性レーダーチャート

- 入力アプリの相性レーダーチャートと同様、**対象プレイヤーごとのレーダーチャートをグリッドで並べて表示**する。
- 対象プレイヤーの選択は、①比較表と同様の独立したチェックボックス方式（`state.compatRadarTargets`。3.1節の選択チップとは別管理）。半荘数上位8名（`COMPAT_RADAR_DEFAULT_TARGET_COUNT`）を初期選択の目安とし、チップで自由に増減できる。
- 各カードの内容：
  - タイトル＝プレイヤー名
  - 軸＝そのプレイヤーが同卓した相手（同卓実績がない相手は軸から除外。対象は選択中のプレイヤー集合に限定）
  - 値＝「そのプレイヤーの平均得点pt－相手の平均得点pt」（`computeHeadToHead`により、確定済み半荘で実際に同卓した範囲のみを集計）。プラスなら勝ち越し、マイナスなら負け越し
  - 点線の同心円（半径50%位置）＝スコア差0（互角）の基準線
  - チャート下に軸ごとの数値リスト（対戦相手名・pt差・同卓回数）
  - 軸ラベルは12px、レーダー半径は70（viewBox 280×280）に調整し、スマホ画面でもラベルが見切れないよう余白を確保している。
- 同卓した確定済み半荘が1件もないプレイヤーは、カード内に「同卓した確定済み半荘が見つかりませんでした。」と表示する。

---

## 8. ④成績5段階評価（相対評価）

選手ごとの成績を6項目×5段階で相対評価し、③相性レーダーチャートと同じ「個人別カードをグリッドで並べる」形式で表示するセクション。内部に「着順系」「打ち筋系」の切替タブを持つ（`state.rankGradeView`、初期値は`"rank"`）。

### 8.1 評価軸

各タブとも6軸固定（`RANK_GRADE_AXES.rank` / `RANK_GRADE_AXES.style`）。

| タブ | 軸 | 評価の方向 | 備考 |
|---|---|---|---|
| 着順系 | 平均着順 | 低いほど良い | `avgRank`をそのまま使用 |
| | 連対率 | 高いほど良い | トップ率＋2着率 |
| | トップ率 | 高いほど良い | `top` |
| | ラス回避率 | 高いほど良い | 1－ラス率 |
| | ハコ率 | 低いほど良い | `hakoRate` |
| | 日別勝率 | 高いほど良い | 対局日ごとに合計ポイントを集計し、プラスだった日の割合 |
| 打ち筋系 | リーチ率 | 高いほど良い（暫定） | `riichiRate` |
| | 放銃率 | 低いほど良い | `houjuuRate` |
| | 和了率 | 高いほど良い | `agariRate` |
| | 副露率 | 高いほど良い（暫定） | `fuuroRate` |
| | ダマテン率 | 高いほど良い（暫定） | `agariOtherRate`（和了のうちリーチ・副露のどちらも無かった割合） |
| | 流局率 | 低いほど良い（暫定） | 参加した局のうち流局に終わった割合（新規集計フィールド`ryuukyokuRate`） |

「暫定」と付した4項目（リーチ率・副露率・ダマテン率・流局率）の評価方向は打ち筋の評価観点として仮に設定したもので、感覚と合わなければ調整可能。

### 8.2 しきい値（5段階への変換方法）

- 固定の絶対基準ではなく、**選択中の対象プレイヤーの実績範囲を5等分した相対基準**を軸ごとに算出する（`computeDynamicBands`）。
- 対象プレイヤーの中の最良値〜最悪値を均等に5分割し、該当する区間によって1〜5点を割り当てる。そのため、対象プレイヤーの中の最上位者は必ずその軸で5点、最下位者は必ず1点になる。
- 対象プレイヤー全員が同値の場合は、5段階とも同じ実測値として表示し、評価は3点で揃える（`degenerate`扱い）。
- しきい値は対象プレイヤーの選択（8.3節）や期間・曜日フィルタ（2.4節）を変えるたびに再計算される。

### 8.3 対象プレイヤーの選択

- チェックボックス方式（`state.rankGradeTargets`）で、**しきい値算出の基準**と**カード表示対象**を兼ねる。着順系・打ち筋系の両タブで選択状態を共有する。
- 初期選択は半荘数上位8名（`RANKGRADE_DEFAULT_TARGET_COUNT`。対象8人以下なら全員）。
- **打ち筋系タブに限り**、総局数（`kyokuCount`）が0のプレイヤー（局ごとの詳細記録が無く、リーチ率等が全項目0%になってしまう選手）は、チェックの有無にかかわらずしきい値算出・カード表示から自動的に除外する。除外が発生した場合はカード群の下の注記に対象外の選手名を明示する。着順系タブは半荘の最終着順があれば成立するため、この除外は行わない。
- 選択した対象プレイヤーの中に、そのタブで評価可能なプレイヤーが1人もいない場合は、専用のメッセージ（「対象プレイヤーを1人以上選択してください。」または「対象プレイヤーの中に、局ごとの詳細記録（総局数）があるプレイヤーがいません。」）を表示する。

### 8.4 合計値・順位の扱い

- カード見出し・選手別実測値表に表示する「合計」「◯人中◯位」は、**表示中のタブに関わらず常に「着順系」の6軸評価（最大30点）を基準に算出する**（打ち筋系タブを見ていても、着順系タブと同じ合計・順位が表示される）。
- この合計・順位の母数（「◯人中」の◯）は、打ち筋系タブでの絞り込み（8.3節の除外）の影響を受けない、選択中の対象プレイヤー全員の人数で固定する。
- 順位は合計が同点の場合は同順位とし、次の順位は同点者数の分だけ繰り下げる（標準的な競技順位方式）。

### 8.5 カードUI

- ③相性レーダーチャートと同じ`radar-grid`のカードUIを流用し、選手ごとに個別のレーダーチャート（6角形、外周＝5点、中心＝1点）を並べて表示する。表示順は8.4節の合計値の降順。
- カード見出し：選手名（19px）と、「合計◯/30（着順系・◯人中◯位）」（本体17px、「（着順系・…）」部分は15pxでやや小さめだが視認できるサイズ）。
- レーダー軸ラベルは12px、半径70（viewBox 280×280）とし、最長5文字のラベル（「ラス回避率」「ダマテン率」）でもスマホ画面で見切れないよう余白を計算して調整している。
- チャート下に軸ごとの実測値と評価点（例：「28.9%（評価5）」）を一覧表示する。

### 8.6 しきい値表・選手別実測値表

- カード群の下に、現在のタブのしきい値（5〜1点それぞれの実測値レンジ）を一覧表示する表を置く。
- さらにその下に、対象プレイヤーの選択に関わらず（打ち筋系は総局数0名を除く）全プレイヤー分の実測値・評価点・着順系合計をまとめた表を表示する。

---

## 9. 技術仕様

- 単一HTMLファイル、Vanilla JS（フレームワーク不使用）。
- 全セクションは`innerHTML`の再描画＋イベント再バインド方式（`render()`を呼ぶたびに`#app`の中身を作り直す）。
- チャート描画はSVG（インラインstyle属性・属性値に直接色を指定）。
- 画像保存機能・CSVエクスポート機能は持たない（①比較表の「表をコピー」（クリップボードへのTSVコピー）のみ）。
- 外部ライブラリ・CDN依存なし。オフラインでも動作する。

### 9.1 入力アプリ（`input-app.html`）への影響

- 本書で追加・変更した内容（④成績5段階評価の新設、レーダーチャート比較・半荘目別推移セクションの削除、セクション番号の詰め直し）は、いずれもビューア側の集計・表示ロジックのみで完結している。
- ④成績5段階評価で新たに使う値（半荘ごとの獲得ポイント平均、日別勝率、流局率など）は、バックアップJSONに既に含まれている情報（`ranks`／`finalPoints`／`records`の局明細）から算出しており、入力アプリ側に新しいデータ項目の追加・入力画面の変更は必要ない。
- そのため、入力アプリ側の改修は不要。
