# 麻雀スタッツ（PC分析ビューア）仕様書

## 1. 概要

| 項目 | 内容 |
|---|---|
| アプリ名 | 麻雀スタッツ（PC分析ビューア） |
| ファイル | `viewer-app.html`（単一HTMLファイル、フレームワーク不使用） |
| 種別 | PC画面を前提にした単一HTMLファイル製Webアプリ |
| 位置づけ | 入力アプリのバックアップJSONを読み込んで表示する、閲覧・分析専用ビューア |
| データ入力 | なし。入力アプリの「データのバックアップ」機能で書き出したJSONファイルを読み込んで表示するのみ |
| 永続化 | なし（`localStorage`不使用）。読み込んだデータはページを閉じる・再読み込みすると消える |
| 入力アプリ本体への影響 | なし。独立したファイルとして提供 |
| 配布・入手方法 | 公開サイトのトップページ（`index.html`）の「📊 分析ビューア」カードから起動できるほか、カード横の「⬇」ボタンで`viewer-app.html`単体をダウンロードすることもできる |

---

## 2. データ読み込み

### 2.1 起動時（未読込状態）

- 画面中央に「ファイルを選択」ボタン＋ドラッグ&ドロップ領域（`#dropzone`）、および「🌐 サイト上のbackup.jsonを読み込む」ボタン（`loadFromServer`）を表示する。
- 対象は入力アプリのバックアップJSON。読み込んだJSONの`app`キーが`"mahjong-stats-backup"`と一致しない場合、またはJSONとして不正な場合はエラーメッセージを表示し読み込みを中止する（この検証・反映処理はファイル選択／ドラッグ&ドロップ／サーバー読み込みの3経路で共通の`processBackupText`関数が行う）。
- 「🌐 サイト上のbackup.jsonを読み込む」は、このビューアが配置されているサイトと同じオリジンの`./backup.json`を`fetch(..., {cache:"no-store"})`で取得し、事前のダウンロード操作なしにその場で読み込む。取得に失敗した場合はローダー画面にエラーメッセージを表示する（読み込み専用。書き込みは行わない）。
- 読み込んだデータはページを閉じる・再読み込みすると消去される旨を画面に明記している。

### 2.2 読込後

- 読込に成功すると、表示状態（`section`・選択チップ・比較表対象・期間フィルタ・相性レーダー対象・統計カードの折りたたみ状態）はすべて初期化される。
- サイドバー最上段に読込サマリを常時表示：書き出し日時（`exportedAt`）／半荘件数（確定済み／全体）／分析対象プレイヤー数（対局・半荘いずれかの実績があるプレイヤー数）。
- サイドバー最下部の「📂 別のファイルを読み込む」ボタンで、確認ダイアログ後にファイル未読込の状態へ戻れる。

### 2.3 集計対象の基準

- 入力アプリの計算ロジック（`computeOverallStats` / `namesForHanchan` / `hanchanGroups` / `computeHanchanKyokuStats` / `computeHeadToHead`）をそのまま移植している。
- 着順系スタッツ（平均順位・トップ率など）は確定済み半荘（`hanchanConfirmed`）のみが対象。
- チョンボ・「結果のみ入力」の局は総局数・和了率・放銃率などの分母から除外する（得点計算のみに影響する）。
- 「統計」セクションのみ`computeOverallStats()`（プレイヤー名リストに登録されているだけで対局実績が無い人も含む）を対象にし、他の4セクションは`computeActiveStats()`（対局実績`kyokuCount>0`または半荘実績`hanchanRankedCount>0`があるプレイヤーのみ）を対象にする。

### 2.4 曜日フィルタ（全セクション共通）

- サイドバーの読込サマリ直下に「すべて／平日／週末」の切替チップ（`state.weekdayFilter`）を1つだけ設置する。土・日を週末と判定する（`isWeekendDate`。入力アプリと共通ロジック）。
- 統計／①比較表／②レーダーチャート／③スコア推移／④相性レーダーチャート／⑤半荘目別推移の**全セクションに共通で適用**される（セクションごとの個別フィルタは持たない）。
- ③スコア推移の期間フィルタ（from/to）とは**AND条件**で組み合わさる（両方の条件を満たす半荘のみが対象になる）。
- サイドバー最上段の読込サマリ（半荘件数：確定済み／全体）は、この曜日フィルタの影響を受けない（読み込んだファイル自体の件数を常に表示する）。
- 別ファイルの読込時（`processBackupText`）は「すべて」にリセットされる。画面表示用の一時状態のみで、保存はされない。

---

## 3. 画面構成

サイドバーナビゲーション形式。左サイドバーで5セクションを切り替え、メインエリアには選択中の1セクションのみを表示する。

```
┌──────────────┬─────────────────────────────┐
│ [読込サマリ]   │                              │
│──────────────│                              │
│ 📊 統計        │                              │
│ ① 比較表       │       メインエリア            │
│ ② レーダーチャート │ （選択中の1セクションを全画面表示） │
│ ③ スコア推移    │                              │
│ ④ 相性レーダーチャート│                        │
│ ⑤ 半荘目別推移   │                              │
│──────────────│                              │
│ [別ファイルを読込] │                          │
└──────────────┴─────────────────────────────┘
```

- 「統計」セクションで対局実績・半荘実績のあるプレイヤーが1人もいない場合は「プレイヤーが登録されていません」を表示する。
- 「統計」以外のセクションで対象プレイヤーが0人の場合は「対局実績のあるプレイヤーがいません。」を表示する。
- 900px以下の画面幅では、サイドバーが上部に横並びレイアウトへ切り替わる簡易レスポンシブ対応がある。

### 3.1 選択チップ（②③⑤の連動）

- メインエリア上部、②レーダーチャート・③スコア推移・⑤半荘目別推移のいずれを表示していても見える位置に「選択中プレイヤー」のチップバー（`renderChipBar`）を表示する（①④では非表示。①は独自のチェックボックス式対象選択、④も独自のチェックボックス式対象選択を持つため）。
- チップに追加された選手には、選択した順に固定パレット（`CHART_PALETTE`、8色）から色を割り当てる（同時ハイライト数が8人を超えると色が循環する）。未選択のプレイヤーは共通のニュートラルグレー（`#B9BEC5`）で表示する。
- チップ操作の起点：
  - ②レーダーチャートの凡例（プレイヤー名ボタン）／ポリゴンのクリック
  - ③スコア推移グラフの凡例／折れ線のクリック
  - ⑤半荘目別推移グラフの凡例／折れ線・点のクリック
  - （②③⑤のどこから操作しても同じ選択状態（`state.chips`）を共有・同期する）
- ①比較表は`state.chips`と連動して、選択中の選手に該当する列の見出し・セルの背景をハイライトするが、①自体からチップの追加・削除操作はできない（①はチェックボックスによる表示対象選択のみ）。
- チップ横の「×」で個別解除、チップバー全体に「選択をクリア」ボタンがある。

---

## 4. 「統計」セクション

入力アプリの半荘集計ロジックを移植した、プレイヤーごとの詳細スタッツカード画面。

1. **全プレイヤー平均値カード**（`📊 全プレイヤー平均値`）：局入力実績（`kyokuCount>0`）のあるプレイヤー全員の打点系スタッツの単純平均。デフォルト最小化、タップで展開。
2. **プレイヤーごとのスタッツカード**（登録済み・実績のある全プレイヤー分。CSS上は`stats-grid`でカード幅360px以上のグリッド表示）：デフォルト最小化。展開すると以下を表示する。
   - **着順**（半荘◯回分と明記）：平均順位／トップ率／2着率／3着率／ラス率／ハコ率
   - **打点**：総局数（詳細）／リーチ数／副露局数／和了数／ツモ数／ロン数／放銃数／リーチ率／副露率／和了率／放銃率／リーチ後和了率／リーチ後放銃率／副露後和了率／副露後放銃率
3. **用語説明**：和了・放銃・副露・チョンボの簡易定義をカード下部に表示。

「総局数（詳細）」は、`resultType`が`chombo`（チョンボ・やり直し）でも`resultonly`（結果のみ入力）でもない、局の詳細を伴う対局のみを数えた値。

---

## 5. ①比較表

- 対象プレイヤーをチップ（チェックボックス）で選択する。対局・半荘実績のあるプレイヤーが6人（`COMPARE_DEFAULT_TARGET_COUNT`）以下の場合は初期状態で全員を選択済みにし、7人以上の場合は半荘数（`hanchanRankedCount`）上位6人を初期選択とする。チップで自由に増減できる（人数の上限はない）。
- 行＝指標（`COMPARE_METRICS`：総局数／半荘数／平均順位／トップ率／2着率／3着率／ラス率／和了率／放銃率／リーチ率／副露率／リーチ後和了率／リーチ後放銃率／副露後和了率／副露後放銃率）、列＝選択した選手。
- 先頭の「指標」列は`position:sticky; left:0;`で固定し、背景は不透明色（`var(--surface)`／`var(--bg)`）を指定して横スクロール時に他の列の文字が透けて重ならないようにしている。固定列と本体の境目には`box-shadow`で縦の区切り線を入れている。
- 3章の選択チップ（`state.chips`）に含まれる選手の列は、見出しとセルの背景・文字色をチップと同じ色でハイライトする。
- 表右上の「📋 表をコピー」ボタンで、現在選択中の選手・指標をタブ区切りテキストとしてクリップボードにコピーする（Excel等に貼り付け可能）。
- 対象プレイヤーが2人未満の場合は「①比較表を表示するには、対局実績のあるプレイヤーが2人以上必要です。」のみを表示する。

---

## 6. ②レーダーチャート比較

- 軸は**和了率／トップ率／ラス率（反転）／放銃率（反転）**の4軸。ラス率・放銃率は低いほど外側（良い）になるよう反転して描画する。
- 各軸の正規化基準は「対局実績のある全プレイヤー中の最大値」。
- 初期状態は全プレイヤーのポリゴンをニュートラルグレー・細線で同時表示する。
- 3章の選択チップに入っている選手だけ、固有色・太線でハイライト表示する（選択中のポリゴンは描画順としても手前（配列の末尾）に来るよう並べ替えている）。
- 凡例（プレイヤー名ボタン）のクリック、またはポリゴン自体のクリックでも選択チップをON/OFF可能（①③⑤とは異なり①には連動しない。②③のみ相互連動）。
- 対象プレイヤーが2人未満の場合は比較表と同様のメッセージのみを表示する。

---

## 7. ③スコア推移

- 入力アプリの累計得点グラフをPC向けに移植・拡大したもの。
- 期間フィルター（`<input type="date">`のfrom/to）で対象の確定済み半荘を絞り込み可能。「指定なし」ボタンでクリアできる。サイドバーの曜日フィルタ（2.4節）ともAND条件で組み合わさる。
- 対象期間に登場する全プレイヤー名について、確定済み半荘の得点pt（`finalPoints`）を日付順に累積した折れ線を描画する（期間より前の半荘の得点は含めない＝期間の先頭で0からの推移になる）。
- 3章の選択チップと連動：未選択はニュートラルグレーの細線、選択中は固有色の太線。凡例・折れ線のクリックでチップをON/OFF可能。
- 対象期間に確定済み半荘が1件もない場合は「対象期間に確定済みの半荘がありません。」のみを表示する。

---

## 7.5. ⑤半荘目別推移

- 「同日内で半荘を重ねるほど成績がどう変わるか」を見るための分析パターン。③スコア推移（時系列の累計推移）とは軸の意味が異なり、横軸は日付ではなく**その日の第何半荘目か**（入力アプリの「半荘No」。日付が変わるたびに1から振り直される）。
- 対象は確定済み半荘（`hanchanConfirmed`）のみ。各半荘のスコアpt（`finalPoints`。オカ・ウマ・チョンボ罰則を反映した値）を、同じ「第◯半荘目」ごとに全期間分まとめて単純平均し、1人1本のトレンド線として描画する（`computeSessionScoreTrend`）。
- 横軸の目盛りには「第◯半荘目」に加えて、その回数目が全期間で何件発生したか（`n=`）を併記する。縦軸はスコアptの平均（0を基準線として表示）。
- 3章の選択チップと連動：未選択はニュートラルグレーの細線、選択中は固有色の太線＋強調ドット。凡例・折れ線・点のクリックでチップをON/OFF可能（②③と同じ`state.chips`を共有）。
- あるプレイヤーがその回数目のデータを持たない日は単純にその点を欠落させる（前後の実在する点同士を直接結ぶ）。
- 対象となる確定済み半荘が1件もない場合は「対象となる確定済み半荘がありません。」のみを表示する。

---

## 8. ④相性レーダーチャート

- 入力アプリの相性レーダーチャートと同様、**対象プレイヤーごとのレーダーチャートをグリッドで並べて表示**する。
- 対象プレイヤーの選択は、①比較表と同様の独立したチェックボックス方式（`state.compatRadarTargets`。②③の選択チップとは別管理）。半荘数上位8名（`COMPAT_RADAR_DEFAULT_TARGET_COUNT`）を初期選択の目安とし、チップで自由に増減できる。
- 各カードの内容：
  - タイトル＝プレイヤー名
  - 軸＝そのプレイヤーが同卓した相手（同卓実績がない相手は軸から除外。対象は選択中のプレイヤー集合に限定）
  - 値＝「そのプレイヤーの平均得点pt－相手の平均得点pt」（`computeHeadToHead`により、確定済み半荘で実際に同卓した範囲のみを集計）。プラスなら勝ち越し、マイナスなら負け越し
  - 点線の同心円（半径50%位置）＝スコア差0（互角）の基準線
  - チャート下に軸ごとの数値リスト（対戦相手名・pt差・同卓回数）
- 同卓した確定済み半荘が1件もないプレイヤーは、カード内に「同卓した確定済み半荘が見つかりませんでした。」と表示する。

---

## 9. 技術仕様

- 単一HTMLファイル、Vanilla JS（フレームワーク不使用）。
- 全セクションは`innerHTML`の再描画＋イベント再バインド方式（`render()`を呼ぶたびに`#app`の中身を作り直す）。
- チャート描画はSVG（インラインstyle属性・属性値に直接色を指定）。
- 画像保存機能・CSVエクスポート機能は持たない（①比較表の「表をコピー」（クリップボードへのTSVコピー）のみ）。
- 外部ライブラリ・CDN依存なし。オフラインでも動作する。
